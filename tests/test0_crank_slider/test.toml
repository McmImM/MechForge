name = "Crank-slider position analysis"
description = "Standalone crank-slider, outputs CSV with header"


[build]
command = "cmake -S .. -B ../build && cmake --build ../build --target test0_crank_slider"


[run]
command = "../build/test0_crank_slider"


[validate]
kind = "txt"
expected = "expected.csv"
